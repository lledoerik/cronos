<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necessaris per al widget -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Cronos"
            tools:targetApi="31">

        <!-- Activitat principal -->
        <activity
                android:name="com.example.cronos.MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme.Cronos"
                android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget petit -->
        <receiver
                android:name="com.example.cronos.CronosWidget"
                android:label="@string/widget_name"
                android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/widget_info" />
        </receiver>

        <!-- Widget gran -->
        <receiver
                android:name="com.example.cronos.CronosWidgetLarge"
                android:label="@string/widget_name_large"
                android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/widget_info_large" />
        </receiver>

        <!-- Widget estil Apple (canvia de color segons l'hora) -->
        <receiver
                android:name="com.example.cronos.CronosWidgetApple"
                android:label="@string/widget_name_apple"
                android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/widget_info_apple" />
        </receiver>

        <!-- Tick per minut: una sola alarma que actualitza tots els widgets -->
        <receiver
                android:name="com.example.cronos.CronosWidgetTickReceiver"
                android:exported="true">
            <intent-filter>
                <action android:name="com.example.cronos.ACTION_WIDGET_TICK" />
            </intent-filter>
        </receiver>

        <!-- Receiver per reiniciar el widget després d'un reinici del dispositiu -->
        <receiver
                android:name="com.example.cronos.BootReceiver"
                android:enabled="true"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

    </application>

</manifest>